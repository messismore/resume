{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "resumed --output index.html --theme jsonresume-theme-felixdeiters",
    "ci": "npm run build"
  },
  "dependencies": {
    "jsonresume-theme-felixdeiters": "file:./theme",
    "resumed": "^3.0.0"
  }
}
